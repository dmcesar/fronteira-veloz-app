{
  "name": "FRONTEIRAVELOZ",
  "version": "0.0.1",
  "scripts": {
    "postinstall": "npm run build",
    "start": "ui5 serve --accept-remote-connections=true --port=$PORT --open='/index.html'",
    "build": "ui5 build --config=ui5.yaml --clean-dest --dest dist --include-task=generateManifestBundle"
  },
  "dependencies": {
    "@ui5/cli": "^2.11.2"
  },
  "devDependencies": {
    "shx": "^0.3.3",
    "@ui5/cli": "^2.11.2",
    "karma": "^6.3.4",
    "karma-chrome-launcher": "^3.1.0",
    "karma-coverage": "^2.0.3",
    "karma-ui5": "^2.3.4",
    "npm-run-all": "^4.1.5",
    "eslint": "^7.29.0"
  }
}
